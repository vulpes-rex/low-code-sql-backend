# Main application database (choose one)
DB_TYPE=mongodb  # or postgres, mysql, sqlite
MONGODB_URI=mongodb://localhost:27017/low-code-sql  # if using mongodb
DB_HOST=localhost  # if using postgres/mysql
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=low_code_sql

# JWT Configuration
JWT_SECRET=your-super-secret-key-change-in-production
JWT_EXPIRATION=1h

# Database Connection Pool Settings
DB_POOL_MIN=2
DB_POOL_MAX=10
DB_POOL_IDLE_TIMEOUT=30000

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS Configuration
CORS_ORIGIN=*

# Logging Configuration
LOG_LEVEL=debug

# SSL Configuration (if needed)
SSL_ENABLED=false
SSL_KEY_PATH=
SSL_CERT_PATH=

# Rate Limiting
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX=100

# Cache Configuration
CACHE_TTL=3600
CACHE_MAX_ITEMS=1000

# WebSocket Configuration
WS_PORT=3001
WS_PATH=/ws

# Query Builder Configuration
QUERY_BUILDER_MAX_DEPTH=5
QUERY_BUILDER_MAX_JOINS=10